import React from "react";
import styled from "styled-components";
import { Container } from "../atoms/global";

const Article = () => {
  return <ArticleBlock>AuthForm</ArticleBlock>;
};

const ArticleBlock = styled(Container)``;

export default Article;
