import React from "react";
import styled from "styled-components";
import { Container } from "../atoms/global";

const Signup = () => {
  return <SignupBlock>AuthForm</SignupBlock>;
};

const SignupBlock = styled(Container)``;

export default Signup;
