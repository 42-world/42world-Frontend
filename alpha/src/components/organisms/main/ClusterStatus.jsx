import styled from "styled-components";

function ClusterStatus() {
  return <ClusterStatusWrapper>클러스터 현황</ClusterStatusWrapper>;
}

const ClusterStatusWrapper = styled.div`
  width: 100%;

  background: red;
`;

export default ClusterStatus;
